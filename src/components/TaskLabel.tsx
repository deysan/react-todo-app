import styled from 'styled-components';

const TaskLabel = styled.label`
  position: relative;
  display: flex;
  align-items: center;
  margin-right: 0.5rem;
  font-size: 1rem;
  color: #fff;
  cursor: pointer;
  transition: 0.2s;
`;

export default TaskLabel;
